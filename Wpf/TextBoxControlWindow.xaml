<Window x:Class="Wpf.TextBoxControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        mc:Ignorable="d"
        Title="TextBoxControlWindow" Height="160" Width="280">
    <StackPanel>
        <StackPanel Margin="10">
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" Text="Hello you!" SpellCheck.IsEnabled="True" Language="en-US" />
        </StackPanel>
        <DockPanel Margin="10">
            <TextBox Name="txtSelect" SelectionChanged="TextBox_SelectionChanged" DockPanel.Dock="Top" />
            <TextBox Name="txtStatus" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True" />
        </DockPanel>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Name="button" Margin="10,0" Content="Select 2 to 5" Click="button_Click" />
            <Button Name="button2" Margin="10,0" Content="Add Prout at 4" Click="button2_Click" />
        </StackPanel>
        
    </StackPanel>
</Window>
